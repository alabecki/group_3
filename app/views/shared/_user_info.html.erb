
  <%= image_tag @current_user.avatar.url(:thumb) %>
<h1><%= current_user.username %></h1>
<span><%= link_to "View my profile", current_user %></span>
<span><%= pluralize(current_user.microposts.count, "Comments") %></span>
